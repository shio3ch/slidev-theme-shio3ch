{
  "name": "slidev-theme-shio3ch",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "npx @slidev/cli dev --theme ./theme",
    "build": "npx @slidev/cli build --theme ./theme",
    "export": "npx @slidev/cli export --theme ./theme"
  }
}
